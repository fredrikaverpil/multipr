name: go

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  ci:
    uses: fredrikaverpil/github/.github/workflows/go.yml@main
    with:
      config-source: golden-latest
